# Template only: do not store real secrets in this committed file.
# Create a local, untracked `.env` file for development values.
# Use with Flutter compile-time defines:
# flutter run --dart-define-from-file=.env

# Example values for local `.env`:
# SUPABASE_URL=YOUR_SUPABASE_URL
# SUPABASE_ANON_KEY=YOUR_SUPABASE_ANON_KEY
