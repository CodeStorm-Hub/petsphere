org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# Android Gradle Plugin / Gradle require a supported JDK (17-21).
# Keep JDK selection environment-driven (Flutter/Android Studio) to avoid
# hardcoded OS-specific paths that fail across machines.
