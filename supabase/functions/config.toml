# Supabase Edge Functions Configuration

[waitlist-signup]
# Allow anonymous access (no JWT required)
verify_jwt = false

# Invoked by Database Webhooks on `notifications` INSERT (service role in header).
[push-fcm]
verify_jwt = false
